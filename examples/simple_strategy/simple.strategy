name: SimpleStrategy
version: "0.0.1"
description: "Example of a simple strategy model"
markets: [
    Market(Binance('MY_API_KEY', 'MY_API_SECRET'), Pair('ADA-USDT')),
    Market(AscentEX('MY_API_KEY', 'MY_API_SECRET'), Pair('ADA-USDT'))
]
parameters: [
    order_refresh_time : int = 10  # Parameter description #,
    max_spread : float = 2.0 # Parameter description #,
    (dynamic) spread : float = 0.5 # Parameter description #
]
